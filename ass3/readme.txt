Small Shell
===========

To compile:
```
$ gcc smallsh.c -o smallsh
```

For improved diagnostics:
```
$ gcc smallsh.c -Wall -Wpedantic -g -o smallsh
```

Alternately, if clang is installed, just run:
```
$ make
```
